export const $=(selector,root=document)=>root.querySelector(selector);
export const $$=(selector,root=document)=>Array.from(root.querySelectorAll(selector));
export const byId=id=>document.getElementById(id);
export const escapeHTML=value=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[char]));
export const formatCurrency=value=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(Number(value)||0);
export const formatNumber=value=>new Intl.NumberFormat("en-IN").format(Number(value)||0);
export const formatDate=value=>{if(!value)return"—";const date=new Date(`${String(value).slice(0,10)}T00:00:00`);return Number.isNaN(date.getTime())?"—":date.toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"});};
export const formatDateTime=(date,time="")=>{if(!date)return"—";const value=new Date(`${String(date).slice(0,10)}T${time||"00:00"}`);return Number.isNaN(value.getTime())?formatDate(date):value.toLocaleString("en-IN",{day:"2-digit",month:"short",year:"numeric",hour:"2-digit",minute:"2-digit"});};
export const todayISO=()=>{const d=new Date();return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;};
export const isToday=value=>String(value||"").slice(0,10)===todayISO();
export const isOverdue=value=>{if(!value)return false;return new Date(`${String(value).slice(0,10)}T23:59:59`)<new Date();};
export const debounce=(fn,delay=250)=>{let timer;return(...args)=>{clearTimeout(timer);timer=setTimeout(()=>fn(...args),delay);};};
export const generateAvatar=name=>{const parts=String(name||"").trim().split(/\s+/).filter(Boolean);return parts.length>1?`${parts[0][0]}${parts[parts.length-1][0]}`.toUpperCase():(parts[0]||"?").slice(0,2).toUpperCase();};
export const validateEmail=value=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(value||"").trim());
export const validatePhone=value=>/^[+]?[-\d\s()]{10,20}$/.test(String(value||"").trim());
export const sanitizeNumber=value=>{const number=Number(String(value??"").replace(/,/g,""));return Number.isFinite(number)?number:0;};
export const sortBy=(records,field,direction="asc")=>[...records].sort((a,b)=>{let x=a?.[field],y=b?.[field];if(field==="value"){x=Number(x)||0;y=Number(y)||0;}else if(field.includes("Date")){x=new Date(x||0).getTime();y=new Date(y||0).getTime();}else{x=String(x??"").toLowerCase();y=String(y??"").toLowerCase();}return(x>y?1:x<y?-1:0)*(direction==="desc"?-1:1);});
export const getDateFilterRange=filter=>{const now=new Date();const start=new Date(now.getFullYear(),now.getMonth(),now.getDate());if(filter==="today")return{start,end:new Date(start.getTime()+86400000)};if(filter==="week")return{start:new Date(start.getTime()-6*86400000),end:new Date(start.getTime()+86400000)};if(filter==="month")return{start:new Date(now.getFullYear(),now.getMonth(),1),end:new Date(now.getFullYear(),now.getMonth()+1,1)};return null;};
export const downloadFile=(filename,content,type)=>{const blob=new Blob([content],{type});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);};
export const csvEscape=value=>{const text=String(value??"");return/[",\n\r]/.test(text)?`"${text.replace(/"/g,'""')}"`:text;};
export const objectToCSV=(rows,columns)=>{const header=columns.map(column=>csvEscape(column.label||column.key)).join(",");const body=rows.map(row=>columns.map(column=>csvEscape(row[column.key])).join(",")).join("\n");return body?`${header}\n${body}`:header;};
export const parseCSV=text=>{const rows=[];let row=[],cell="",quoted=false;for(let i=0;i<String(text).length;i++){const char=text[i],next=text[i+1];if(char==='"'){if(quoted&&next==='"'){cell+='"';i++;}else quoted=!quoted;}else if(char===","&&!quoted){row.push(cell);cell="";}else if((char==="\n"||char==="\r")&&!quoted){if(char==="\r"&&next==="\n")i++;row.push(cell);if(row.some(v=>v.trim()!==""))rows.push(row);row=[];cell="";}else cell+=char;}if(cell!==""||row.length){row.push(cell);if(row.some(v=>v.trim()!==""))rows.push(row);}if(!rows.length)return[];const headers=rows[0].map(v=>v.trim());return rows.slice(1).map(values=>Object.fromEntries(headers.map((header,i)=>[header,String(values[i]??"").trim()])));};
export const showToast=(message,type="success")=>{let container=byId("toastContainer");if(!container){container=document.createElement("div");container.id="toastContainer";container.className="toast-container";document.body.appendChild(container);}const toast=document.createElement("div");toast.className=`toast toast-${type}`;toast.innerHTML=`<span>${type==="success"?"✓":type==="error"?"✕":type==="warning"?"⚠":"ⓘ"}</span><span>${escapeHTML(message)}</span><button type="button">×</button>`;toast.querySelector("button").onclick=()=>toast.remove();container.appendChild(toast);setTimeout(()=>toast.remove(),4000);};
export const confirmAction=(message,title="Confirm Action")=>new Promise(resolve=>{const overlay=document.createElement("div");overlay.className="modal-overlay active";overlay.innerHTML=`<div class="modal modal-small"><div class="modal-header"><h2>${escapeHTML(title)}</h2><button type="button" class="modal-close-btn">×</button></div><div class="modal-body"><p>${escapeHTML(message)}</p></div><div class="modal-footer"><button type="button" class="btn btn-secondary cancel-confirm">Cancel</button><button type="button" class="btn btn-danger accept-confirm">Confirm</button></div></div>`;document.body.appendChild(overlay);const finish=result=>{overlay.remove();resolve(result);};$(".accept-confirm",overlay).onclick=()=>finish(true);$(".cancel-confirm",overlay).onclick=()=>finish(false);$(".modal-close-btn",overlay).onclick=()=>finish(false);overlay.onclick=e=>{if(e.target===overlay)finish(false);};});
